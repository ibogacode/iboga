'use client'

// Re-export useUser from the context for backward compatibility
// All components using this hook will now share the same user state
export { useUser } from '@/contexts/user.context'


